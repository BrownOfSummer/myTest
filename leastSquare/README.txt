// implement of leaseSquare by matlab and c
leastSquare.c leaseSquare.m polyfit a line of y = ax + b
multi_leastSquare.c .m polyfit a curve: y = a0 + a1*x + a2*x^2 + ...
the result is same as polyfit(x, y, n), only the result sequence is reverse
